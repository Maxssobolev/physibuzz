const initialState = {
    user: {
        name: 'Jon Walton'
    }

}

export const appReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}